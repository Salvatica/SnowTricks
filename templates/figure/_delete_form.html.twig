<form method="get" id="trickDeleteForm" action="{{ path('figure_delete', {'slug': figure.slug}) }}"
      onsubmit="return confirm('Etes-vous certain de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ figure.slug) }}">

    {% block bouton %}
    {% endblock bouton %}

</form>
