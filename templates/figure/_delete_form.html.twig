<form method="get" id="trickDeleteForm" action="{{ path('figure_delete', {'id': figure.id}) }}"
      onsubmit="return confirm('Etes-vous certain de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ figure.id) }}">

    {% block bouton %}
    {% endblock bouton %}

</form>
