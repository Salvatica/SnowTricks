


<form method="post" id="trickDeleteForm" action="{{ path('figure_delete', {'id': figure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ figure.id) }}">

    {% block bouton %}
    {% endblock bouton %}

</form>
